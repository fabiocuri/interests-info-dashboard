apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: interests-info-dashboard-data
  labels:
    app: interests-info-dashboard
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # storageClassName omitted -> minikube's default ("standard", hostpath).
