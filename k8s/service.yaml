apiVersion: v1
kind: Service
metadata:
  name: interests-info-dashboard
  labels:
    app: interests-info-dashboard
spec:
  type: ClusterIP
  selector:
    app: interests-info-dashboard
  ports:
    - name: http
      port: 8000
      targetPort: http
