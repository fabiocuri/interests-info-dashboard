services:
  dashboard:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATA_DIR: /data
    volumes:
      - dashboard-data:/data
    restart: unless-stopped

volumes:
  dashboard-data:
